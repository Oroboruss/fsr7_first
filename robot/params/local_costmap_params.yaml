local_costmap:
   #global_frame: /map
   #global_frame: odom
   robot_base_frame: base_link
   update_frequency: 2.0
   publish_frequency: 3.0
   static_map: false
   rolling_window: true
   width: 2.0
   height: 2.0
   resolution: 0.05
   transform_tolerance: 0.3

   observation_sources: scan #ukkelit
   scan: {data_type: LaserScan, sensor_frame: laser, topic: /scan, expected_update_rate: 0.3, marking: true, clearing: true}
   #ukkelit: {data_type: PointCloud2, sensor_frame: camera_rgb_optical_frame, topic: /ukkelit, expected_update_rate: 0.0, marking: true, clearing: true}
